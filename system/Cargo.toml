[workspace]
members = [
    "core",
    "alg",
    "app",
    "proto/collector",
    "proto/tracker"
]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.149"
